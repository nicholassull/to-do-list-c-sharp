Create a function that takes an array of numbers and return both the minimum and maximum 
numbers, in that order.

Examples
minMax([1, 2, 3, 4, 5]) ➞ [1, 5]

minMax([2334454, 5]) ➞ [5, 2334454]

minMax([1]) ➞ [1, 1]

Notes
All test arrays will have at least one element and are valid.

Sudo
Function([Array])
min = first number
max = first number
Array.for each
check: if active element is smaller than min
        if it is, min = element

array.for each
check: if active element is larger than max
        if it is, max = element 

min.join(max)

Javascript
function minMax(array){
  let min = array[0];
  let max = array[0];

  array.forEach(function(element) {
    if (element < min) {
      min = element;
    } else {
      if (element > max) {
      max = element;
    }
  });
  let arrayMin = min.toString().toArray();
  let arrayMax = max.toString().toArray();
  const outputArray = min.join(max); ('');

  let temp = array.sort((a, b) => {
    return a - b;
  });

  const outputArray = [min, max];
  return outputArray;
}